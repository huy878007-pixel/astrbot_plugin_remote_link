# 变更日志

## v0.1.1（2026-08-21）— 提示词注入与产物送达修复

一轮实战排查（群内真实使用暴露的 4 个连锁问题），全部附带可运行的回归检查。

### 🐛 修复

- **正/负提示词注入反转**（图上出现乱码文字水印、生成内容与需求无关）
  根因：负框内容启发式 `NEG_LIKE_RE` 含 `nsfw`，而正面提示词框（尤其 NSFW 场景）本身就常含 `nsfw/explicit/naked` → 正面框被误判成负框 → 正负完全对调，负面词（`watermark/text/signature`）进了正面框，等于要求模型画出文字水印。
  修复：`NEG_LIKE_RE` 去掉 `nsfw`，只认 `worst quality/bad anatomy` 等明确质量词。

- **产物缓存跨子目录串图**（群里收到的图 ≠ 本地实际生成的图）
  根因：ComfyUI 不同子目录（`Anima_v7` / `Anima_v10`）会产出同名文件（`%date%` 未替换时文件名固定），而云端 `_pull_media` 缓存只按 `Path(filename).name` 命名 → 先入缓存的旧图被同名新请求命中。
  修复：缓存文件名加 `subfolder` 前缀，不同子目录的同名产物互不覆盖。

- **生成请求被误判为「取历史产物」**（新任务直接回上次的旧图）
  根因：取产物的自然语言分支跑在生成判定之前，且排除词表只有 `换/再来/重新…` 短名单 →「帮我生成一张图片」同时含产物词与「给/发」类动词被判成索取历史产物；同时生成判定要求「含产物词」，「生成一位…少女，手持一把枪械」这类无 `图/画` 字样的需求被漏判。
  修复：取产物分支改为复用 `_is_direct_generation_command`（不再维护第二份关键词表）；生成判定去掉「含产物词」限制（@ 机器人 + 生成动词开头即为生成意图）。

- **引用（回复）消息里的图片取不到**（图生视频退化成文生视频）
  根因：群内常见用法是「先发一张图 → 引用它 → 说要求」，图片在 `Reply` 组件的 `chain` 里而不在顶层消息链；`_extract_event_images` 只遍历顶层 → 图片数为 0，`pick_subtype` 因此误判成文生类。
  修复：递归 `Reply.chain`（当前消息的图优先、按来源去重、深度上限 3 层）；并在协议端 `get_msg` 失败、`chain` 为空时通过 `_fetch_reply_images` 用消息 ID 兜底补拉。

- **`ROLE_HINTS_KEY` 被误判为缺失参数**（报「工作流中存在未提供的参数: YUNXIN_ROLE_HINTS」）
  修复：`convert_ui_to_api` 改为返回 `(prompt, role_hints)` 元组，标签信息不再塞进工作流 dict，从根上避开占位符检查正则。

### ✨ 新增

- **提示词框节点标签**：在 ComfyUI 里把节点 Title 改成「正面提示词」/「负面提示词」（或含 `positive`/`negative`），插件 100% 按标签注入，不依赖内容猜测——不同工作流、不同节点 ID、空提示词框都准确。没打标签的工作流行为不变。
- **`tools/tag_workflows.py` 自动打标工具**：本地跑一次即为 ComfyUI 所有工作流的正/负提示词节点打上标签；按内容自动区分，识别不了的列出待确认，打标前自动备份 `*.bak_tag`。

### ✅ 回归检查

- `test/test_gen_vs_fetch.py` — 11 条真实生成语料 + 4 条取产物语料的判定
- `test/test_cache_subfolder.py` — v7/v10 同名产物不串、二次命中缓存
- `test/test_reply_images.py` — 引用图提取、顺序、去重、`get_msg` 兜底、端到端子分类路由
- 冒烟 `smoke_plugin` / `smoke_agent` 全过

## v0.1.0（2026-08-20）— 发布整理

- 🐛 修复两处方法签名错误（发布冒烟测试发现）：`_extract_json` 缺少 `self`（提示词增强/路由解析必崩）；`_param_intent_detected` 误用 `@staticmethod` 却声明了 `self`（图片生成带参数时崩溃）
- 📦 新增 `scripts/build_release.py` 一键打包脚本（插件 zip / 源码 zip / 本地代理发行版 zip）
- 🔧 本地 GUI「打开输出目录」不再硬编码机器路径，按配置的 `comfyui.userdata_dir` 自动推导
- 📜 补齐开源协议合规：`THIRD-PARTY-NOTICES.md`（LiteGraph.js MIT 全文、依赖清单、pystray LGPL 说明），并补回 `litegraph.js/css` 被压缩掉的上游版权声明
- ✅ 冒烟测试适配当前架构（智能调度/异步受理/指令校验），全部通过

## v0.1.0（2026-08-17）— 正式版

对外版本号统一为 **v0.1.0**（此前 0.2.x / 0.3.x 为内部迭代，并入本版）。

- 🧭 两级流水线：一级分类（image/video/audio/text）→ 子分类路由（文生图/图生图/画面分析/文生视频/单图生视频/多图生视频/音频生成，手动关键词优先 + 图数规则）→ 子分类配置表（各行独立默认工作流 + LLM + 提示词模板）→ 二级提示词增强 → 自动注入 → 产物直发 QQ
- 🎬 Minimax H3 视频工作流拆分：整合流程拆为 文生视频 / 单图生视频 / 多图生视频 三个独立工作流，各配独立 VHS 输出前缀与 `__IMAGE__` 上传占位；修复 AudioConditioning 的 COMBO 显示名（如 "T2VA — 文生音视频" → "T2VA"）；自动注入扩展识别 AudioConditioning 类节点的 prompt 输入
- 🐛 转换器 COMBO 归一化：UI→API 转换时自动把 COMBO 显示名还原为枚举值（兼容新旧两种 COMBO 规格），前端保存的 "T2VA — 文生音视频" 等显示名不再导致 `value_not_in_list`
- 🎨 Web 控制台：左侧边栏导航 + 深色设计系统，总览/工作流/预设/设置分区；设置页子分类配置卡片、提供商模型联动、NSFW 开关、通知开关
- 🖥️ 本地 GUI：无边框自绘标题栏（猫娘 Logo + 连接状态点）、五区导航（总览/配置/历史/日志/设置）、系统资源监控（CPU/内存/GPU）、生成历史、日志搜索/过滤/导出、完成桌面通知、可配置总览卡片、深色风格化滚动条
- 🗂️ 系统托盘：点 ✕ 默认最小化到托盘（代理后台继续运行），托盘菜单「显示主窗口 / 退出」；开机自启直接进托盘；修复无边框窗口最小化后任务栏图标消失的问题
- 🔧 修复：配置页/历史页布局被按钮挤占不铺满、日志字体回退日文、exe 打包缺 ttkbootstrap 资源

### 历史内部迭代（并入 v0.1.0）

## v0.3.0（2026-08-16）— 子分类路由 · Web 控制台大换新

- 🧭 两级流水线正式落地：一级分类（image/video/audio/text，LLM 或关键词兜底）→ 子分类规则（手动关键词 > 图数：带图→图生类、多图→多图生视频）→ 子分类配置表 → 二级提示词分析（子分类专属 LLM/模板）→ 自动注入 → 直发 QQ
- 🎯 子分类配置表：文生图/图生图/画面分析提示词/文生视频/单图生视频/多图生视频/音频生成，每行独立配置默认工作流 + LLM 提供商/模型 + 提示词模板（{policy}）；兼容迁移 default_workflows
- 🎨 Web 控制台大换新：左侧边栏导航（品牌猫娘 Logo）四区、深色高级感设计系统 v4、顶栏随页切换；设置页新增子分类配置卡片、提供商模型联动下拉；移除全局提示词增强模板（子分类各行独立）
- 🔧 后端重构：删除旧的"LLM 选工作流"决策路径，改为分类+规则；`_resolve_router_provider` 支持按子分类指定提供商

## v0.2.0（2026-08-16）— 直发产物 · 两级流水线 · 性能优化

- 📤 产物直发：插件用 context.send_message 主动把图片/视频发到会话，不再依赖机器人 LLM 决定是否发送（v4.27 工具运行器默认只缓存图片让 LLM「审核后发送」，经常不发送）
- 🧭 两级 LLM 流水线：第一级只做任务分类（image/video/audio/text）→ 用默认工作流（或按产物类型匹配）→ 第二级专门做提示词分析（自然语言 → 英文 tag）；兼容旧决策格式
- 🎞️ 产物去重：有正式产物时自动跳过 PreviewImage 的临时预览文件
- ⚡ 页面性能：切出工作流页暂停 LiteGraph 渲染（修空转卡顿）；快照轮询 5s→10s 且页面隐藏时跳过；设置保存/加载更顺畅

## v0.1.8（2026-08-16）— 免改造工作流：自动提示词注入

- 🪄 任意工作流免改造：没有 __PROMPT__/__NEGATIVE__ 占位符的工作流也能直接调用——代理端自动识别正/负提示词框（CLIPTextEncode 等文本节点，按内容启发式区分正负框）并自动填入增强后的提示词
- 🧠 智能调度对"含文本节点但无占位符"的工作流同样触发提示词增强（此前要求工作流带占位符才增强）

## v0.1.7（2026-08-16）— 工作流识别与默认工作流

- 🤖 初始化按钮：工作流 Tab 新增「LLM 识别工作流」，云端 LLM 自动分析本地全部工作流的用途，给每个工作流打标签（类别 image/video/audio + 子类 文生图/图生图/文生视频/图生视频/音频/其他 + 中文说明 + 3~6 个标签），结果持久化在配置里
- 🎯 默认工作流：设置页新增配置——图片/视频/音频各指定一个默认工作流；智能调度（LLM 路由清单提示 + 规则降级加分）在用户未指定工作流时自动使用默认
- 📋 工作流列表显示识别标签、说明与「⭐默认图/视频/音频」徽标

## v0.1.6（2026-08-16）— 自定义提示词模板

- 📝 提示词模板可自定义：设置页新增「调度决策提示词」与「提示词增强模板」两个可编辑模板（带一键恢复默认）；增强模板支持 {policy} 占位符（NSFW 策略注入点）
- ✨ 内置精心调校的默认模板：调度决策（JSON 决策规范）+ 提示词增强（强制英文 tag + 质量词结构）

## v0.1.5（2026-08-16）— 英文 tag 强化 · 预设工具路径增强

- 🌐 提示词增强强制英文 tag：扩散模型文本编码器对英文 tag 最稳；增强器输出含中文时自动追加"翻译成英文 tag"重试
- 🐛 修复：自然聊天时 LLM 调预设工具（remote_workflow_<名字>）路径完全绕过提示词增强的问题——现在预设工具、/remote wf run、智能调度三条路径都走同一套增强
- 💬 工具结果附上实际使用的提示词，减少机器人配文张冠李戴（魔理沙事件）

## v0.1.4（2026-08-16）— 全路径提示词增强 · 节点级实时流程

- 🧠 `/remote wf run` 路径也接入提示词增强（此前只有智能调度路径增强，直跑预设时自然语言原文直接当提示词）
- 📊 节点级实时进度：代理上报当前执行节点（node/node_type），云端控制台概览进度条显示节点名，工作流页图上**当前节点高亮发光**，本地 GUI 进度条同步
- 🧹 清理误上传的乱码实验工作流文件

## v0.1.3（2026-08-16）— 提示词增强 · NSFW 开关 · 实时进度

- 🧠 自然语言 → 提示词增强：智能调度路径中，路由 LLM 选好工作流后，专门的提示词工程步骤把用户的自然语言描述改写成英文 tag 形式的正/负提示词（质量词自动补全）；LLM 不可用时用原始描述 + 默认负提示词兜底
- 🔞 NSFW 开关（设置页）：关闭时提示词增强强制 SFW 并在负提示词中加入屏蔽词；开启后允许成人内容
- 🔔 接单先回复开关（notify_on_start，默认开）：/remote 指令与 LLM 工具接到任务先回一句「已提交/正在分析」
- 📊 实时进度：云端控制台概览页新增进度条（隧道 progress 消息实时上报）；本地 GUI 状态区新增任务进度条
- 🐛 修复 UI→API 转换四连：① 前端专属节点（MarkdownNote/分组旁路器）孤儿跳过；② ComfyUI v0.33 新格式 object_info 兼容；③ control_after_generate 控制项错位；④ VHS 类对象形态 widgets_values；⑤ 按节点序列化 inputs 数组转换（widget 升级为输入槽也能正确映射）；⑥ mode=4（rgthree 绕过）排除 + 连线穿透
- 🐛 修复 _parse_presets 只认数组格式导致保存的预设不生效

## v0.1.2（2026-08-16）— 修复预设保存不生效

- 🐛 修复：`_parse_presets` 只解析数组格式，而保存接口写入的是对象格式，导致从设置页保存预设后永远回退到内置 txt2img（预设从未真正生效）；现同时兼容对象/数组两种格式

## v0.1.1（2026-08-16）— 兼容性与控制台改版

- 🔧 兼容旧版 AstrBot（v4.24.x）：`self.logger` 改为模块级 logger，插件可在不提供该属性的旧版本上正常加载
- 🎨 云端控制台四页合一：概览 / 工作流 / 预设 / 设置 合并为单页 Tab 导航，切换即用
- 🔙 新增「← 返回」按钮：一键回到 AstrBot 主面板
- 👁 认证 token 支持「点开才显示」（默认隐藏，点击眼睛图标切换）
- 🖼️ 设置页后端新增返回完整 token 字段（`settings/get` → `auth_token`），仅供设置页按需展示

## v0.1.0（2026-08-16）— 首个发布版本

### 核心能力

- ☁️→🖥️ 反向隧道：本地代理主动拨出、断线自动重连，NAT 后免穿透
- 🧠 LLM 智能调度：能力清单动态发现（工作流/参数/checkpoint/LLM 模型/GPU/队列），LLM 路由自动选工作流、挑模型、填参数，规则匹配兜底
- 🖥️ ComfyUI 驱动：占位符参数注入、约定入口节点注入（图片 base64/文本槽）、负种子随机化、WS 推送等待（老版本轮询兜底）、400 错误友好化、产物收集（图/视频/GIF/音频/ShowText 文本）
- 🤖 本地 LLM：OpenAI 兼容代理（含 SSE 流式），可把本地模型直接配置为 AstrBot 提供商
- 🎛️ 工作流预设：插件配置 JSON 化、每个预设自动生成 LLM 工具、保存热更新
- 📊 云端控制台（AstrBot 插件页面）：概览（SSE 实时事件流）/ 工作流可视化（LiteGraph 只读渲染，UI/API 双格式）/ 预设配置 / 设置页（路由 LLM 选择、token 管理、工具开关、媒体清理）
- 🖥️ 本地 GUI（YunxinAgent.exe）：vapor-dark 主题、状态区实时刷新、可视化配置表单、实时日志、本地看板（127.0.0.1:8899）
- 🔧 指令：`/remote status|ping|wf list|wf presets|wf run|comfyui list|queue|gen|llm|shell|do`
- 🔧 LLM 工具：`remote_local_compute`（万能智能入口）/ `remote_llm_chat` / `remote_shell` / `remote_workflow_<预设>`

### 工程

- 测试：UI→API 转换器单测、WS 驱动端到端（假 ComfyUI）、插件端 9 项冒烟、代理端 6 项冒烟
- 打包：PyInstaller onefile（含图标、ttkbootstrap 主题、内置工作流模板）
- 文档：README（职责划分/安全/故障排查）、DESIGN.md（二期规划）
